"use client";import{useState}from"react";import Link from"next/link";import{BrandMark,Button,Field,Input}from"../../components/ui";export default function Forgot(){const[email,setEmail]=useState("");const[done,setDone]=useState(false);const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setDone(true);setLoading(false)}return <main className="canvas-wash min-h-screen flex items-center justify-center px-4"><div className="w-full max-w-md card p-7"><BrandMark size="lg" title="Yasser" subtitle="Account recovery"/><h1 className="mt-6 text-2xl font-bold text-ink">Forgot password</h1>{done?<p className="mt-3 text-sm text-ink-2">If that account exists, a reset email is on its way. <Link className="font-semibold text-brand" href="/login">Return to sign in</Link>.</p>:<form className="mt-6 space-y-5" onSubmit={submit}><Field label="Email" htmlFor="email"><Input id="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></Field><Button variant="primary" className="w-full" loading={loading}>{loading?"Sending…":"Send reset link"}</Button></form>}</div></main>}
