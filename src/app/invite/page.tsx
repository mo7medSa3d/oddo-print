"use client";import{useState}from"react";import{useSearchParams}from"next/navigation";import Link from"next/link";export default function Invite(){const token=useSearchParams().get("token")??"";const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/team/invitations/accept",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,email})});const d=await r.json();setMsg(r.ok?"Invitation accepted. Sign in to continue.":d.error??"Invitation failed")}return <main className="canvas-wash min-h-screen flex items-center justify-center px-4"><div className="w-full max-w-md card p-7"><h1 className="text-2xl font-bold text-ink">Accept workspace invitation</h1><p className="mt-2 text-sm text-ink-3">Use the email address the invitation was sent to.</p><form className="mt-6 space-y-4" onSubmit={submit}><input className="w-full rounded-lg border border-edge bg-surface p-3" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><button className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white">Accept invitation</button></form>{msg&&<p className="mt-4 text-sm text-ink-2">{msg}</p>}<Link className="mt-5 inline-block text-sm font-semibold text-brand" href="/login">Sign in</Link></div></main>}
