"use client";
import { useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { BrandMark, Button, Field, Input, ErrorState } from "../../components/ui";
export default function Signup(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[err,setErr]=useState("");const[done,setDone]=useState(false);const[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setErr("");try{const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error??"Registration failed");setDone(true);router.replace(`/verify-email?email=${encodeURIComponent(email)}`);}catch(e){setErr(e instanceof Error?e.message:"Registration failed")}finally{setLoading(false)}}return <main className="canvas-wash min-h-screen flex items-center justify-center px-4 py-12"><div className="w-full max-w-md"><BrandMark size="lg" title="Yasser" subtitle="Customer account"/><div className="mt-6 card p-6"><h1 className="text-2xl font-bold text-ink">Create your account</h1><p className="mt-1 text-sm text-ink-3">Use your business email. A verification link is required.</p>{done?<div className="mt-5 rounded-xl border border-edge-accent bg-brand-subtle p-4 text-sm text-ink-2">Check your email for the verification link. <Link className="font-semibold text-brand" href="/login">Return to sign in</Link>.</div>:<form className="mt-6 space-y-5" onSubmit={submit}><Field label="Email" htmlFor="email"><Input id="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required/></Field><Field label="Password" htmlFor="password"><Input id="password" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password" minLength={12} required/></Field>{err&&<ErrorState title="Could not create account" message={err}/>}<Button type="submit" variant="primary" className="w-full" loading={loading}>{loading?"Creating…":"Create account"}</Button><p className="text-center text-sm text-ink-3">Already have an account? <Link className="font-semibold text-brand" href="/login">Sign in</Link></p></form>}</div></div></main>}
