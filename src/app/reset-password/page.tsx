"use client";import{useState}from"react";import{useSearchParams,useRouter}from"next/navigation";import Link from"next/link";import{BrandMark,Button,Field,Input,ErrorState}from"../../components/ui";export default function Reset(){const token=useSearchParams().get("token")??"";const router=useRouter();const[pw,setPw]=useState("");const[err,setErr]=useState("");const[done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setErr("");const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:pw})});const d=await r.json();if(!r.ok){setErr(d.error??"Reset failed");return}setDone(true);setTimeout(()=>router.replace("/login"),700)}return <main className="canvas-wash min-h-screen flex items-center justify-center px-4"><div className="w-full max-w-md card p-7"><BrandMark size="lg" title="Yasser" subtitle="Account recovery"/><h1 className="mt-6 text-2xl font-bold text-ink">Reset password</h1>{done?<p className="mt-3 text-sm text-ink-2">Password changed. <Link href="/login" className="font-semibold text-brand">Sign in</Link>.</p>:<form className="mt-6 space-y-5" onSubmit={submit}><Field label="New password" htmlFor="password"><Input id="password" type="password" value={pw} onChange={e=>setPw(e.target.value)} minLength={12} autoComplete="new-password" required/></Field>{err&&<ErrorState title="Could not reset password" message={err}/>}<Button variant="primary" className="w-full">Change password</Button></form>}</div></main>}
